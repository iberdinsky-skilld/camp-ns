$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json

name: Badge
status: experimental
group: DrupalCamp

props:
  type: object
  properties:
    icon:
      type: string
      title: Icon Name
      enum:
        - difficulty
        - knife
        - serves
        - timer

slots:
  text:
    type: string
    title: Text
    examples:
      - Hello all!
